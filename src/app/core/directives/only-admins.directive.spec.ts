import { OnlyAdminsDirective } from './only-admins.directive';

describe('OnlyAdminsDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyAdminsDirective();
    expect(directive).toBeTruthy();
  });
});
